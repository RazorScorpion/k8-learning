apiVersion: v1
kind: Service
metadata:
  name: java-service
spec:
  ports:
  - port: 9091
    nodePort: 9092
    targetPort: java-port
    protocol: TCP
  selector:
    app: java-docker-project
  type: NodePort
